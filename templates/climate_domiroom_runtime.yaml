---
sensor:
  - default_entity_id: sensor.climate_domiroom_runtime
    unique_id: climate_domiroom_runtime
    name: Climate Domiroom Runtime
    device_class: duration
    unit_of_measurement: min
    icon: >
      {% if is_state('climate.domiroom', 'cool') %}
        mdi:timer-play-outline
      {% else %}
        mdi:timer-off-outline
      {% endif %}
    state: >
      {% if is_state('climate.domiroom', 'cool') %}
        {{ (now().timestamp() - as_timestamp(states.input_datetime.climate_domiroom_start_time.state)) / 60 }}
      {% else %}
        0
      {% endif %}
