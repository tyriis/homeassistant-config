---
sensor:
  - default_entity_id: sensor.climate_alexroom_runtime
    unique_id: climate_alexroom_runtime
    name: Climate Alexroom Runtime
    device_class: duration
    unit_of_measurement: min
    icon: >
      {% if is_state('climate.alexroom', 'cool') %}
        mdi:timer-play-outline
      {% else %}
        mdi:timer-off-outline
      {% endif %}
    state: >
      {% if is_state('climate.alexroom', 'cool') %}
        {{ (now().timestamp() - as_timestamp(states.input_datetime.climate_alexroom_start_time.state)) / 60 }}
      {% else %}
        0
      {% endif %}
