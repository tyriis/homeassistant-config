---
create_lock:
  alias: "Create Lock"
  sequence:
    - service: rest_command.create_lock
      data:
        key: "{{ key }}"
        owner: "{{ owner | default('Home Assistant') }}"
        duration: "{{ duration | default('1h') }}"
  mode: single
