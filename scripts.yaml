room_scene_switch_transition:
  alias: "Switch Room Scene with transition"
  sequence:
    - service: scene.turn_on
      target:
        entity_id: "{{ scene_entity }}"
      data:
        transition: "{{ transition | default(5) }}"
